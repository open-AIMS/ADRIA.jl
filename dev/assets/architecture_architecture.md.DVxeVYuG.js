import{_ as a,o as s,c as t,an as i}from"./chunks/framework.BmuutBn3.js";const u=JSON.parse('{"title":"Architectural overview","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/architecture.md","filePath":"architecture/architecture.md","lastUpdated":null}'),n={name:"architecture/architecture.md"};function o(r,e,l,d,p,c){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="Architectural-overview" tabindex="-1">Architectural overview <a class="header-anchor" href="#Architectural-overview" aria-label="Permalink to &quot;Architectural overview {#Architectural-overview}&quot;">​</a></h1><p>ADRIA consists of three overarching components:</p><ol><li><p>a set of Multi-Criteria Decision Analysis methods used to emulate decision-makers</p></li><li><p>a coral ecosystem model (referred to as ADRIAmod)</p></li><li><p>a suite of analysis and assessment methods</p></li></ol><p>Each component may be applied separately, or altogether to perform an end-to-end analysis. This page documents the underlying architectural implementation of ADRIA, detailing how the above components interact.</p><h2 id="General-Structure" tabindex="-1">General Structure <a class="header-anchor" href="#General-Structure" aria-label="Permalink to &quot;General Structure {#General-Structure}&quot;">​</a></h2><p>The primary purpose of ADRIA is to support reef restoration and adaptation through the development of robust intervention strategies under deep uncertainty. Here, &quot;robustness&quot; refers to the ability of an intervention strategy to meet desired outcomes under uncertain future conditions, which themselves are unknown and may be unexpected. To do so, ADRIA adopts an Exploratory Scenario Modelling framework to explore the range of possible futures and their outcomes.</p><p>Exploratory Scenario Modelling (ESM) itself leverages uncertainty and sensitivity analysis (UA/SA). Uncertainty analysis quantifies the variability of uncertainties in a given system and its expected range of outcomes. Sensitivity analysis examines the effect of a change in a model&#39;s inputs to its outputs. Common workflows to such analyses involve a three-step process (as discussed in Pianosi et al., 2016):</p><ol><li><p>Input sampling</p></li><li><p>Model evaluation</p></li><li><p>Post-processing</p></li></ol><p>When ADRIA is applied in its entirety, &quot;input sampling&quot; is analogous to scenario generation: all model factors (the inputs) are collated and are sampled through a quasi-monte carlo process. The Sobol&#39; sampling method is adopted as the default, although any method provided by the <a href="https://github.com/SciML/QuasiMonteCarlo.jl" target="_blank" rel="noreferrer">QuasiMonteCarlo.jl</a> package can be used. Sample adjustment is required to map sampled values (which are continuous) to categorical or whole number values (e.g., Baroni and Tarantola, 2014) as may be expected by some factors. Values are also adjusted to avoid implausible factor combinations, such as active intervention factors in the case of non-intervention scenarios.</p><p>Model evaluation is simply running the model with the generated scenario set.</p><p>Post-processing is the analysis and visualization step.</p><h2 id="Model-Factors" tabindex="-1">Model Factors <a class="header-anchor" href="#Model-Factors" aria-label="Permalink to &quot;Model Factors {#Model-Factors}&quot;">​</a></h2><p>Factors in ADRIA are defined across four sub-components:</p><ol><li><p>Intervention</p></li><li><p>CriteriaWeights</p></li><li><p>EnvironmentalLayer</p></li><li><p>Coral</p></li></ol><p>Each sub-component is represented by a struct with fields for each parameter. The <code>Intervention</code> sub-component holds parameters that define a given adopted intervention strategy/option: how many (and type of) corals are to be seeded, the length of any deployment, the start/end years, and so on. When a 5D DHW dataset (containing MCB durations and albedo levels) is loaded, ADRIA dynamically populates additional MCB-specific intervention factors (prefixed with <code>mcb_</code>). These factors are used in a &quot;Temporal Splicing&quot; approach, where the environmental conditions switch between a baseline and a treated slice according to the specified intervention strategy. See <a href="/ADRIA.jl/dev/usage/generating_scenarios#Marine-Cloud-Brightening-(MCB)-Scenarios">Marine Cloud Brightening (MCB) Scenarios</a> for more details. The <code>CriteriaWeights</code> sub-component relates to the preferences for the Multi-Criteria Decision Analysis methods, further detailed in <a href="/ADRIA.jl/dev/concepts/dMCDA#Dynamic-Multi-Criteria-Decision-Analysis">Dynamic Multi-Criteria Decision Analysis</a>. For the ADRIA ecosystem model (ADRIAmod), <code>EnviromentalLayer</code> relate to the environmental scenarios available for a given simulation (a time series of DHW and Wave stress), itself determined on the loading of data (see <a href="/ADRIA.jl/dev/usage/scenario_runs#Running-scenarios">Running scenarios</a>).</p><p>The <code>Coral</code> sub-component relates to ADRIAmod, currently representing six coral species:</p><ol><li><p>Arborescent Acropora</p></li><li><p>Tabular Acropora</p></li><li><p>Corymbose Acropora</p></li><li><p>Corymbose non-Acropora</p></li><li><p>Small massives and encrusting</p></li><li><p>Large massives</p></li></ol><p>ADRIAmod represents these across six size classes, with six parameter sets for each coral species and size class. These six parameter sets are further detailed in <a href="/ADRIA.jl/dev/architecture/architecture#Model-Factors">Model Factors</a>, however, it results in a large number of unique factors (6 groups x 6 size classes x 6 parameters: 216 factors). Instead of specifying all coral factors by hand, ADRIA instead auto-generates the sub-component using a common template (see <code>coral_spec()</code> and <code>create_coral_struct()</code> in <a href="/ADRIA.jl/dev/API#General-API">General API</a>). Through discussion with expert stakeholders, factor bounds were set to +/- 10% of their default values following a triangular distribution, the peak of which is the default value.</p><p>The <a href="https://github.com/rafaqz/ModelParameters.jl" target="_blank" rel="noreferrer">ModelParameters.jl</a> package is used to provide a simple table-like interface to model factors. ADRIA provides a wrapper to the <code>Param</code> type provided by ModelParameters.jl called a <code>Factor</code>. These <code>Factor</code>s requires ADRIA-specific metadata to be provided alongside the default value for a given model factor. These include the assumed distribution of the factor, distribution factors (principally the lower and upper bounds), and a human-readable name and description. An example from the <code>Intervention</code> sub-component is shown below.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">guided</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Param</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Factor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ptype</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ordered categorical&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    dist</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CategoricalDistribution,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    dist_params</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Guided&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    description</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Intervention effort level: -1 no intervention (counterfactual), 0 unguided, 1 guided (MCDA-driven).&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Note that factor values are provided as floats - even where discrete values are expected - to maintain type stability. Mixing floats with integers will lead to an error. Similarly, values used to update the model should always be floats.</p><p>Combinations of the realized factor values then represent a &quot;scenario&quot;.</p><div class="tip custom-block"><p class="custom-block-title">Parameter Collation and Scenario Generation</p><p>See <a href="/ADRIA.jl/dev/usage/cookbook#Cookbook-examples">Cookbook examples</a> for an example how-to on collating model factors and generating samples.</p></div><h3 id="Conditional-sampling-dependencies" tabindex="-1">Conditional sampling dependencies <a class="header-anchor" href="#Conditional-sampling-dependencies" aria-label="Permalink to &quot;Conditional sampling dependencies {#Conditional-sampling-dependencies}&quot;">​</a></h3><p>Many model factors only matter under a subset of scenario regimes — seeding deployment factors have no effect on a counterfactual scenario, and criteria weights have no effect unless a <code>guided</code> MCDA approach is active. Sampling every factor unconditionally would waste sample budget on combinations that can&#39;t affect model output, and would dilute sensitivity/PAWN analyses with dead dimensions. ADRIA instead resolves these dependencies automatically, as early as it safely can:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>   model spec (all factors)</span></span>
<span class="line"><span>            |</span></span>
<span class="line"><span>            v</span></span>
<span class="line"><span>  +----------------------+</span></span>
<span class="line"><span>  |  fix known-inactive   |   pre-sampling: factors are fixed to a</span></span>
<span class="line"><span>  |  factors to constants |   constant and never sampled at all</span></span>
<span class="line"><span>  +----------------------+</span></span>
<span class="line"><span>            |</span></span>
<span class="line"><span>            v</span></span>
<span class="line"><span>     QMC sampling (Sobol&#39; / LHS / ...)</span></span>
<span class="line"><span>            |</span></span>
<span class="line"><span>            v</span></span>
<span class="line"><span>  +----------------------+</span></span>
<span class="line"><span>  |  zero/adjust factors  |   post-sampling: fixes applied per-row,</span></span>
<span class="line"><span>  |  that depend on drawn |   using each scenario&#39;s own drawn values</span></span>
<span class="line"><span>  |  values               |</span></span>
<span class="line"><span>  +----------------------+</span></span>
<span class="line"><span>            |</span></span>
<span class="line"><span>            v</span></span>
<span class="line"><span>      scenario DataFrame</span></span></code></pre></div><p>The split exists because not every dependency can be resolved before sampling — some only depend on a setting fixed for the whole call (e.g. <code>guided</code> in <code>sample_cf</code>), which can be resolved up-front, while others depend on a value that&#39;s only known once a row has actually been sampled (e.g. whether a <em>drawn</em> <code>fogging</code> value happens to be <code>0</code>), which can only be resolved afterwards, row by row.</p><div class="tip custom-block"><p class="custom-block-title">Where this lives</p><ul><li><p><code>src/io/sampling/sampling_dependencies.jl</code> — pre-sampling: fixes spec columns to constants before sampling begins.</p></li><li><p><code>src/io/sampling/sampling_transforms.jl</code> — post-sampling: zeroes/adjusts sampled values row-wise, in <code>_apply_transforms!</code> (ordering here is load-bearing — see its docstring).</p></li></ul></div><p>Both stages share the same declarative rule format — a table of parent → child rules, each checking whether <code>child</code> is active given a known <code>parent</code> value:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Field</th><th style="text-align:right;">Meaning</th></tr></thead><tbody><tr><td style="text-align:right;"><code>parent</code></td><td style="text-align:right;">Factor whose value the rule checks</td></tr><tr><td style="text-align:right;"><code>child</code></td><td style="text-align:right;">Factor (or named group of factors) to fix if inactive</td></tr><tr><td style="text-align:right;"><code>op</code></td><td style="text-align:right;">Comparison applied to <code>parent</code>&#39;s value (e.g. <code>:eq</code>, <code>:gt</code>)</td></tr><tr><td style="text-align:right;"><code>value</code></td><td style="text-align:right;">Threshold/comparison value passed to <code>op</code></td></tr><tr><td style="text-align:right;"><code>negate</code></td><td style="text-align:right;">Invert the result of <code>op</code>, for rules more naturally stated as &quot;inactive when...&quot;</td></tr><tr><td style="text-align:right;"><code>fix_to</code></td><td style="text-align:right;">Constant <code>child</code> is set to when inactive</td></tr></tbody></table><p>A fixed-point loop applies these rules pre-sampling (a fixed child can itself gate further rules), and <code>_validate_dependencies</code> checks the table for unknown parents/ops/children, cycles, and rules that would fix the same column to conflicting values. To add a new dependency: if it depends on <code>guided</code> and is knowable before sampling, add it to <code>sampling_dependencies.jl</code>; otherwise add it to <code>sampling_transforms.jl</code> — see the header comment in <code>sampling_dependencies.jl</code> for a worked example.</p><div class="warning custom-block"><p class="custom-block-title">Effect on Sobol&#39; sampling</p><p>Both pre- and post-sampling adjustment break the low-discrepancy structure of the Sobol&#39; sequence used for scenario generation. Adjusted scenario sets must <strong>not</strong> be treated as valid Sobol&#39; samples for the purpose of estimating Sobol&#39; sensitivity indices. This is why PAWN (Pianosi and Wagener 2015, 2018), a distribution-based method, is used for sensitivity analysis instead of Sobol&#39; indices — see <a href="/ADRIA.jl/dev/usage/generating_scenarios#Generating-scenarios">Generating scenarios</a>.</p></div><h1 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h1><p>Pianosi, F., K. Beven, J. Freer, J. W. Hall, J. Rougier, D. B. Stephenson, and T. Wagener. 2016. Sensitivity analysis of environmental models: A systematic review with practical workflow. Environmental Modelling &amp; Software 79:214–232.</p>`,34)])])}const g=a(n,[["render",o]]);export{u as __pageData,g as default};
